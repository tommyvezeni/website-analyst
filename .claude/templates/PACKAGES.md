# Pacchetti opzionali del sistema di progetto

> Registro dei pacchetti opzionali che l'agente puo proporre quando inizializza o allinea un progetto. Non e' un elenco di cose da installare sempre: ogni voce si offre con un gate esplicito, in base al tipo di progetto, e si istanzia solo su conferma dell'utente. La skill `init-project-system` e i due prompt consultano questo file per sapere cosa proporre.

## Come si usa

Al Passo 4 dell'inizializzazione e durante l'allineamento, l'agente legge questo registro, valuta quali pacchetti sono pertinenti al progetto secondo la colonna "quando offrirlo", e li propone uno per uno con una domanda esplicita, offrendo di istanziarli ora o di rimandarli come promemoria. Non assume mai: anche un pacchetto pertinente si attiva solo se l'utente accetta. Un pacchetto gia presente nel progetto non si reinstalla: se ne mostra la differenza e si chiede come procedere. Allo stesso modo, se il progetto fornisce gia quella capacita in proprio (per esempio implementa gia una propria knowledge base o un proprio sistema di grafo), il pacchetto non si propone come duplicato: al massimo si propone di allineare l'implementazione esistente allo standard. Quando un pacchetto viene attivato, l'agente mostra subito un recap d'uso, cioe i comandi e il flusso essenziali presi dal README del pacchetto, cosi l'utente sa come usarlo da subito.

Per i pacchetti di tipo MCP server vale una regola aggiuntiva: si propone un massimo di tre o quattro server nuovi per sessione, verificando che il totale dei server connessi non superi sei. Ogni server espone tool che occupano token nel contesto a ogni turno, anche se non vengono usati. Si preferisce sempre l'implementazione ufficiale del vendor (GitHub per GitHub, Sentry per Sentry) rispetto a fork community non verificati: un audit pubblico ha trovato problemi di sicurezza nel 66% dei server MCP scansionati. I server che recuperano contenuto web o documenti esterni sono vettori di prompt injection e vanno esaminati prima di concedere permessi di scrittura.

## Catalogo

| Pacchetto | Cosa fa | Quando offrirlo | Cosa istanzia | Note |
|---|---|---|---|---|
| `latex` | Ambiente di build LaTeX: manifesto pacchetti, script setup/build, skill `latex-build` | Il progetto contiene o produrra file `.tex` | `templates/latex/` in `scripts/`, `tex-packages.txt`, `.latexmkrc`, `.claude/skills/latex-build/` | Script `.ps1` e `.sh`; la distribuzione TeX resta esterna e non versionata |
| `diagrams` | Resa dei diagrammi Mermaid `.mmd` in `.svg` riusando il browser di sistema | Il progetto ha o avra diagrammi sotto `.claude/context/diagrams/` | `templates/tools/render-diagrams.mjs` in `tools/render-diagrams.mjs` | Richiede Node e un browser Chromium-based (Edge o Chrome) |
| `code-context` (MCP) | Server MCP tree-sitter che da struttura cartelle e simboli del codice | In allineamento di un progetto esistente, per far mappare all'agente la struttura del codice e popolare le schede | `.mcp.json` in radice, dalla variante OS (`templates/mcp.json` o `templates/mcp.windows.json`) | Avviato via `npx`, zero dipendenze native; nessuna cartella `mcp/` |
| `claude-code-handoff` | Riferimento distillato e versionato delle opzioni di Claude Code (flag CLI, comandi di sessione, permessi, MCP, skills, subagent, hooks) con auto-aggiornamento dalla guida community: script dual-OS che confronta la versione upstream, scarica la fonte e ri-distilla via `claude -p`, comando `/refresh-handoff`, workflow GitHub Actions opzionale | Qualsiasi progetto in cui si vuole che agente e utente abbiano sotto mano le capacita' dello strumento senza riscoprirle a ogni sessione; si propone all'inizializzazione e quando l'utente chiede "quali opzioni offre Claude Code" | `templates/claude-code-handoff/` in `.claude/context/claude-code-handoff.md`, `.claude/commands/refresh-handoff.md`, `tools/update-handoff.ps1`/`.sh` (variante OS), opzionale `.github/workflows/update-handoff.yml`; stato generato in `.claude/handoff-state.json` (tracciato), fonte grezza in `_notes/upstream/` (ignorata) | Fonte Cranot/claude-code-guide, auto-aggiornata ~ogni 2 giorni quando l'updater upstream gira; gli script rilevano lo stallo della fonte (last_check upstream >14 giorni, gia' osservato) e avvisano; il confronto di versione e' a costo zero, la ri-distillazione e' una corsa LLM; in caso di divergenza dalle regole del sistema vincono le regole |
| `stack-profiles` | Profili di regole pronti per gli stack ricorrenti (ts-mcp, python, react, n8n, generico): convenzioni di stile, comandi tipici e controlli pre-commit, come regola modulare normativa | L'inizializzazione o l'allineamento riconosce uno degli stack coperti dai manifest del progetto (`package.json` con MCP SDK, `pyproject.toml`, dipendenze React, struttura n8n); `generico` e' il fallback esplicito quando nessun profilo corrisponde | Un solo profilo da `templates/stack-profiles/profiles/<profilo>.md` in `.claude/rules/stack-profile.md` | Normativo e complementare alla scheda descrittiva `STACK.md`, che resta il posto dello stack reale; il file istanziato si personalizza col progetto; deriva dal bundle Cranot dell'utente |
| `hooks-starter` | Tre hook di automazione pronti, in doppia forma .ps1/.sh e mai attivi di default: `session-context` (SessionStart: branch, commit, file modificati e testa di memory/index.md nel contesto), `protect-sensitive-files` (PreToolUse su Write/Edit), `secret-scan` (PreToolUse su Bash per i git commit) | Il progetto vuole automatizzare la procedura di ripresa della sezione 12 o concretizzare i guard-rail della sezione 14 di PROJECT-SYSTEM.md; `session-context` e' quello a piu' alto valore e si propone per primo | `templates/hooks-starter/hooks/` in `.claude/hooks/` (variante OS); l'attivazione resta manuale copiando i soli blocchi voluti da `settings.hooks.<os>.json` in `.claude/settings.json` | Blocco via exit code 2 con motivo su stderr; script difensivi (input non parsabile = lascia passare) e di sola lettura; `secret-scan` non scatta finche' il deny di baseline su `git commit` resta in vigore, e' difesa in profondita'; deriva dal bundle Cranot dell'utente |
| `automation-starter` | Copre i livelli headless e routine di Claude Code restando dentro l'uso incluso nei piani Team o Max: wrapper dual-OS per invocare `claude -p` fuori da una sessione interattiva, workflow GitHub Actions opzionale autenticato via token di abbonamento, guida alla terza via nativa `/schedule` | Il progetto vuole automatizzare un task ricorrente (report di drift, controllo periodico, promemoria) senza tenere un terminale aperto, restando nella quota dell'abbonamento invece di aprire una chiave API a consumo | `templates/automation-starter/` in `tools/headless-run.ps1`/`.sh`, opzionale `.github/workflows/automation-routine.yml` + `.github/automation-routine-prompt.txt`; output di ogni corsa in `_notes/automation/` (ignorato) | Pacchetto separato e alternativo a `claude-code-handoff`, che usa invece `ANTHROPIC_API_KEY` a consumo nel proprio workflow per quella singola pipeline: qui l'autenticazione e' sempre `claude login` in locale o `CLAUDE_CODE_OAUTH_TOKEN` da `claude setup-token` su GitHub Actions, mai una chiave API; lo script locale si rifiuta di girare se rileva `ANTHROPIC_API_KEY` nell'ambiente; la terza via nativa (`/schedule`) non richiede istanziazione ma i suoi limiti di quota per piano restano da verificare sull'account dell'utente; originale al template, non derivato dal bundle Cranot |
| `dev-skills` | Quattro skill di sviluppo da scegliere una per una: `test-generator` (test sui pattern e framework gia' presenti), `mcp-tool-scaffold` (tool MCP con contratto, validazione e description orientate all'LLM), `code-review` e `security-review` (varianti di progetto delle revisioni, versionate col repo) | Progetti con sviluppo attivo che vogliono procedure versionate e condivise da un clone; `test-generator` e `mcp-tool-scaffold` non duplicano nulla, le due di review si propongono solo dichiarando la sovrapposizione con le skill native `/code-review` e `/security-review` e con gli agenti di `subagent-template` | `templates/dev-skills/skills/<skill>/` in `.claude/skills/<skill>/`, una per una a scelta al gate | Le skill di review omonime mettono in ombra le native dentro il progetto: scelta esplicita, oppure si istanziano con nome distinto; deriva dal bundle Cranot dell'utente |
| `knowledge-wiki` | LLM Wiki: `sources/` immutabile, `wiki/` compilata dall'LLM, schema `WIKI-SCHEMA.md`, skill `wiki-digest` | Progetti dove si accumula conoscenza trasversale nel tempo, se non gia coperta da una knowledge base nativa | `templates/knowledge-wiki/` in `knowledge/` (`WIKI-SCHEMA.md`, `sources/`, `wiki/`, `log.md`) e `.claude/skills/wiki-digest/` | Pattern Karpathy; vedi knowledge-systems-analysis |
| `book-to-skill` | PDF tecnico in skill pre-digerita on-demand, via la skill `book-digest` | Progetti con libri o PDF tecnici di riferimento | `templates/book-to-skill/` in `.claude/skills/book-digest/`; le skill-libro `<slug>/` le genera `book-digest`, locali per default, globali solo su conferma | Pattern book-to-skill; ponte opzionale verso `knowledge-wiki` (path A/B) |
| `docx-to-docs` | Converte un `.docx` voluminoso in un albero `docs/` Markdown navigabile e versionato: un file per sezione, `README.md` indice generati che linkano ai figli, hub `DEVELOPMENT.md`; conversione deterministica e idempotente con annotazioni (banner LEGACY), redazioni e pulizia `--clean` | Il progetto parte da un `.docx` o da un corpus scritto a mano da rendere documentazione tecnica navigabile; complementa l'ingestione a fette, che invece non versiona. Per piu documenti insieme, non per la conversione in documentazione, vedi `doc-ingest` | `templates/docx-to-docs/` in `tools/docx-to-md.py` (+ `annotations.json`); output in `docs/`; `redactions.json` locale | Richiede `python-docx`; il `.docx` resta locale come fonte di rigenerazione; verifica di completezza e report integrati |
| `doc-ingest` | Ingestione incrementale di un corpus di documenti voluminosi (`.pdf`, `.docx`, `.pptx`, `.xlsx`, `.html`) in una cache Markdown locale a zero token: manifest a content-hash per non riconvertire l'invariato, indice `_INDEX.md` di Livello 1 (titoli, conteggi, stato) rigenerato a ogni corsa, motore veloce di default con modalita accurata (`--engine docling`) e fallback OCR (`--ocr`) opzionali | Il progetto ha o ricevera piu documenti PDF/DOCX/office voluminosi da consultare durante il lavoro senza versionarli; complementa `docx-to-docs`, che invece produce documentazione versionata da un solo documento | `templates/doc-ingest/` in `tools/doc-ingest.py`; cache locale in `_notes/.tmp-doc-cache/` | Richiede `markitdown`; `docling` e `pytesseract`+`pdf2image` (+ binario `tesseract-ocr`) sono opzionali, solo se si usano i rispettivi flag |
| `notebooklm-bridge` | Metodo di ricerca fondata NotebookLM (gratuito) + Claude: loop a 4 passi (NotebookLM sintetizza con citazioni, Claude ragiona sulla sintesi, rimanda a NotebookLM cio che va verificato, Claude scrive la versione finale), preceduto da una verifica di disponibilita' dell'account Google/browser; tiene il corpus di fonti fuori dal contesto di Claude e vi fa ragionare solo la sintesi citata | Il progetto ragiona o scrive a partire da un corpus di fonti proprie dell'utente e conviene tenerlo fuori dal contesto di Claude; e' l'incrocio di token economy e ricerca. Non si propone se il progetto non ha fonti proprie da caricare o se basta la ricerca web (skill `deep-research`) | `templates/notebooklm-bridge/` in `.claude/skills/notebooklm-bridge/` e `tools/notebooklm-check.ps1`/`.sh` (variante OS); cache locale in `_notes/notebooklm/` | Piano gratuito tassativo (solo browser, nessuna API Enterprise a pagamento); modo manuale conforme di default, modo assistito via `notebooklm-mcp` opt-in; deriva da screenshot di un metodo pubblico condiviso dall'utente, non ancora validato su un progetto reale |
| `notebooklm-mcp` (MCP) | Server MCP della community che pilota il NotebookLM gratuito nel browser via automazione, riusando un profilo Chrome con login Google persistito e leggendo le citazioni dal DOM; abilita il modo assistito del pacchetto `notebooklm-bridge` | Progetti con `notebooklm-bridge` attivo che ripetono il loop spesso e vogliono che Claude piloti NotebookLM invece del copia-incolla manuale; opt-in a ragion veduta | `.mcp.json` in radice con la configurazione del server scelto | Community, NON ufficiale; l'automazione del browser sul piano gratuito vive in zona grigia dei ToS (l'unico endpoint conforme e' l'API Enterprise a pagamento, fuori perimetro). Raccomandato `PleasePrompto/notebooklm-mcp` (MIT, Patchright); alternative `jacob-bd/notebooklm-mcp-cli`, `julianoczkowski/notebooklm-mcp-2026`; in alternativa `playwright-mcp` come automazione generica del browser |
| `academic-researcher` | Ambiente di ricerca accademica: scoping del topic, tracciamento tripartito delle fonti (verificata/da verificare/scartata), sincronizzazione Zotero-JabRef del `.bib`, e la Corpus Analysis Suite (10 prompt collaudati) per analizzare un corpus di paper gia' caricato | Il progetto e' di ricerca accademica, di tesi, o richiede analisi sistematica di un corpus di paper; non si propone se il progetto ha gia' un proprio sistema bibliografico equivalente | `templates/academic-researcher/` in `.claude/skills/` (8 skill), `.claude/rules/no-uncited-claims.md`, `research-vault/` (documento di riferimento, `.bib`, scope) | 5 skill complete (corpus-analysis, citation-tracker, bib-sync, research-scoping, literature-search), 3 parzialmente stub da calibrare all'attivazione (deep-paper-reading, gap-analysis, skill-autogen); si abbina alle righe MCP e tool esterni sotto |
| `learning-agent` | Trasforma il progetto in un tutor di apprendimento guidato: profilo learner con onboarding a bottoni, roadmap con template pedagogico per categoria (lingua, programmazione, materia concettuale), tre subagent (`tutor`, `kb-retriever`, `examiner`) e tre skill/comandi (`/profile`, `/learn`, `/review`) | Il progetto ha come scopo dichiarato l'apprendimento personale di un topic specifico a partire da una knowledge base di file dell'utente, non lo sviluppo di un prodotto; non si propone se il progetto ha gia' un proprio tutor o spaced repetition equivalente | `templates/learning-agent/` in `.claude/agents/` (3 file), `.claude/skills/` (3 skill), `.claude/commands/` (3 comandi), `LEARNER_PROFILE.md` in radice, `.claude/context/learning-agent-reference.md` | Deriva da ricerca dell'utente di luglio 2026, non da un modulo gia' rodato: il livello di orchestrazione e' completo da subito, la scelta dei MCP di knowledge base e Anki resta al gate (righe `knowledge-mcp` e `anki-mcp` sotto) |
| `knowledge-mcp` (MCP) | Knowledge base locale con RAG ibrido vettoriale piu' grafo, basato su LightRAG: ingesta PDF/DOCX/Markdown, estrae entita' e relazioni, espone la ricerca via FastMCP | Progetti con `learning-agent` attivo che vogliono una knowledge base interrogabile sui file dell'utente (libri, dispense); adatto a topic strutturati con dipendenze concettuali | `.mcp.json` in radice con configurazione knowledge-mcp | Community; richiede Python 3.12 e `uv`; repo olafgeibig/knowledge-mcp; alternative: Local Knowledge RAG MCP di patakuti (100% locale via Ollama), mcp-local-rag di nkapila6 (fallback di ricerca web locale) |
| `anki-mcp` (MCP) | Spaced repetition via proxy locale verso AnkiConnect: creazione/modifica note, gestione deck, sessioni di review conversazionali, nessuna telemetria | Progetti con `learning-agent` attivo che vogliono automatizzare lo scheduling del ripasso invece di tracciarlo solo in `LEARNER_PROFILE.md` | `.mcp.json` in radice con configurazione anki-mcp-server | Community; richiede Anki desktop e l'add-on AnkiConnect (plugin id 2055492159); repo ankimcp/anki-mcp-server (ankimcp.ai); alternativa minimale: mcp-ankiconnect di samefarrar (solo il loop di ripasso) |
| `codebase-learning` | Trasforma un progetto finito o quasi finito di sviluppare in un ambiente di apprendimento guidato della repository stessa: comando `/learn-repo` a cinque fasi (panoramica, astrazioni centrali, flussi, design puro, idiomi dello stack) e subagent `code-tutor` che cita sempre path e riga reali, appoggiandosi ai grafi strutturali gia' in catalogo (`serena`, `repomix`, `code-context`, `codebase-memory-mcp`, `graphify`) invece di grep a tappeto | Il progetto e' dichiarato finito o quasi finito di sviluppare e l'utente vuole essere guidato nel capirne l'architettura, i paradigmi e i flussi invece di continuare a modificarlo; non si propone se il progetto e' ancora in sviluppo attivo, o se serve solo un onboarding rapido gia' coperto dalla skill `onboard` | `templates/codebase-learning/` in `.claude/agents/code-tutor.md`, `.claude/skills/learn-repo/`, `.claude/commands/learn-repo.md`, `.claude/context/codebase-learning-reference.md` | Deriva per intero da un handoff di ricerca dell'utente di luglio 2026, non ancora validato su un progetto reale; distinto da `learning-agent`, che insegna un topic esterno da una knowledge base personale, non la repository corrente |
| `serena` (MCP) | Grafo semantico del codice via language server (LSP): navigazione a livello di simbolo (`find_symbol`, `find_references`), non di testo, su 20-40+ linguaggi, riusando gli stessi language server di VS Code; cache semantica dedicata | Progetti con `codebase-learning` o `code-context` attivo dove la ricerca testuale non basta piu' (repo grandi o multi-linguaggio) | `.mcp.json` in radice con configurazione serena, dopo `uv tool install -p 3.13 serena-agent` e `serena init` | MIT; repo oraios/serena; alternativa piu' pesante di `code-context` (tree-sitter), da scegliere quando serve risoluzione semantica cross-file |
| `repomix` (MCP) | Impacchetta l'intera codebase, locale o remota, in un singolo file consolidato ottimizzato per l'AI: metriche, file tree, contenuto formattato | Progetti con `codebase-learning` attivo, come vista consolidata iniziale prima di scendere nel dettaglio con `serena` o il subagent `code-tutor` | `.mcp.json` in radice con configurazione `npx -y repomix --mcp` | MIT; repo yamadashy/repomix (repomix.com) |
| `deepwiki-skill` | Auto-genera documentazione di apprendimento con citazioni precise a riga: pipeline analyze -> generate (subagent in parallelo) -> validate (accuratezza dei riferimenti) | Progetti con `codebase-learning` attivo che vogliono persistere il materiale di apprendimento in `docs/learning/` invece di consumarlo solo in sessione | Skill esterna Claude Code: si clona in `.claude/skills/deepwiki/` (locale al progetto, preferito; globale solo su conferma) | OSS; richiede Python 3.12+; repo natsu1211/deepwiki-skill |
| `deepwiki-mcp` (MCP) | Q&A architetturale in linguaggio naturale su repository GitHub pubbliche gia' indicizzate (codice, README, issue) | Progetti con `codebase-learning` attivo il cui repository e' effettivamente pubblico; mai per codice proprietario, l'indicizzazione pubblica non copre repo privati | `.mcp.json` in radice con `claude mcp add -s user -t http deepwiki https://mcp.deepwiki.com/mcp` | Gratis solo per repo pubblici; endpoint ufficiale mcp.deepwiki.com |
| `caveman` | Riduce ~65% dei token di output facendo rispondere l'agente in modo telegrafico, senza toccare il ragionamento | Sessioni operative o coding output-heavy; NON quando il progetto produce documentazione o prosa | Tool esterno di sessione, installato su conferma (skill/plugin Claude Code); nessun file nel progetto | Vedi `rules/token-economy.md`; repo juliusbrussee/caveman |
| `graphify` | Grafo di conoscenza dell'intero progetto, codice e documenti: relazioni, community, nodi centrali e report di insight, con visualizzazione interattiva | Capire in modo olistico un progetto grande o sconosciuto, o una collezione di documenti; complementare a `code-context` (simboli) e a `knowledge-wiki` (pagine curate) | Tool esterno: `uv tool install graphifyy` o `pipx install graphifyy`, poi `graphify install`; si usa con `/graphify .` | MIT; codice via tree-sitter locale, documenti via LLM (consuma token); repo safishamsi/graphify |
| `arxiv-cli` | CLI standalone (non MCP) per recupero rapido dei paper arXiv piu recenti per categoria/query, senza demone da mantenere | Progetti con `academic-researcher` attivo, come primo passo veloce di `literature-search` o per monitoraggio periodico di un topic | Binario nel PATH, installato via `npm`, `go install` o `cargo install`; invocato da bash, nessun file nel progetto | MIT; repo AstraBert/arxiv-cli; il testo dei paper scaricati resta input non fidato (vedi regola `no-uncited-claims` del pacchetto) |
| `grobid` | Motore di riferimento per trasformare un PDF scientifico in XML/TEI strutturato: titolo, abstract, autori, keyword e parsing dei riferimenti bibliografici (F1-score >0.87) | Progetti con `academic-researcher` attivo che leggono paper in profondita' (skill `deep-paper-reading`); richiede Docker | Container Docker (`docker run --rm -it --init -p 8070:8070 lfoppiano/grobid:0.8.0`); nessun file nel progetto | Open source dal 2011, mantenuto da Luca Foppiano/Inria; usato anche da CORE e da pipeline RAG scientifiche come PaperQA2 |
| `paperqa2` | Framework RAG agentico per letteratura scientifica: risposte con citazioni in-testo verificate, re-ranking contestuale, controllo retrazioni, compatibile con Claude via LiteLLM | Progetti con `academic-researcher` attivo che vogliono risposte su un corpus di PDF con citazioni verificate invece di lettura manuale | Tool esterno installato via pip; nessun file nel progetto | Open source; licenza da verificare all'installazione; repo Future-House/paper-qa; riferimento Skarlinski et al. 2024 |
| `humanizer` | Skill che rimuove i segni di scrittura AI dal testo: frasi riempitive, regola del tre, trattini lunghi e grassetto in eccesso, hedging, tono promozionale (33 pattern, con audit finale) | Progetti che producono prosa o testo (blog, libro, documentazione) | Skill esterna: si clona in `.claude/skills/humanizer/` (locale al progetto, preferito; globale solo su conferma); si usa con `/humanizer <testo>` | MIT; complementa la regola `interaction-style`; repo blader/humanizer |
| `taste-skill` | Skill di design-taste: migliora layout, tipografia, spaziatura e motion delle interfacce generate, per evitare UI dall'aspetto generico-AI | Progetti con frontend o UI | Skill esterna: `npx skills add https://github.com/Leonxlnx/taste-skill` | MIT; repo Leonxlnx/taste-skill |
| `ccusage` | Misura baseline del consumo token per sessione, modello e progetto; legge i JSONL locali di Claude Code senza inviare nulla altrove | Sempre, prima di installare qualsiasi altro strumento di ottimizzazione — serve la baseline per misurare i guadagni reali | Nessun file nel progetto; `npx ccusage@latest` per report; `/usage` e `/context` per la sessione corrente | MIT; repo ryoppippi/ccusage |
| `rtk` | Comprime l'output di git, cargo, pytest, npm e oltre 100 comandi shell prima che entri nel contesto; 60-90% di risparmio sui comandi rumorosi | Progetti con test runner o CI output-pesanti su Linux o WSL2 (su Windows nativo l'auto-riscrittura richiede WSL) | Tool esterno: installer curl; hook `PreToolUse` in `settings.json` | MIT; repo rtk-ai/rtk |
| `ponytail` | Impone la disciplina "senior developer pigro" al codice generato: YAGNI, stdlib prima, minimo che funziona; 46-94% meno righe sui benchmark Haiku 4.5 | Sessioni coding output-intenso su modelli Haiku/Sonnet/Opus; non usare su modelli locali piccoli | Plugin Claude Code: `/plugin marketplace add DietrichGebert/ponytail` poi `/plugin install ponytail@ponytail` | MIT; repo DietrichGebert/ponytail |
| `codebase-memory-mcp` | Grafo strutturale del codice (call chain, rotte HTTP, dipendenze tra moduli, 158 linguaggi via tree-sitter); 10x-120x meno token su query strutturali rispetto all'esplorazione file-by-file | Progetti oltre 50K righe dove la navigazione strutturale e' frequente; complementare a `code-context` (simboli) e `graphify` (grafo concettuale) | MCP server: binario statico locale, zero dipendenze; `claude mcp add codebase-memory -- <percorso-binario>` | MIT; repo DeusData/codebase-memory-mcp |
| `context-mode` | Sandboxa l'output di tool pesanti (Playwright, log server, dump issue) in SQLite locale; Claude vede solo un riferimento invece del dump grezzo | Sessioni con output di tool che superano regolarmente le decine di migliaia di token | Tool esterno di sessione; nessun file nel progetto | MIT; repo mksglu/context-mode |
| `headroom` | Comprime tutto cio che arriva all'LLM (output tool, log, file, storia conversazione) tramite proxy locale; i dati non lasciano la macchina; 47-95% secondo workload | Sessioni operative molto lunghe e eterogenee dove RTK e igiene di sessione non bastano | Tool esterno: `pip install headroom-ai[all]`; avviato con `headroom wrap claude` | MIT; repo chopratejas/headroom |
| `claude-code-router` | Instrada ogni richiesta verso il modello piu economico per quel tipo di task (Haiku per task banali, Sonnet per lavoro normale, Opus per casi complessi); 3-5x risparmio sul traffico spostato | Il vincolo principale e' la bolletta API; verificare la policy di compliance prima di abilitare provider non-Anthropic su dati soggetti a NDA | Tool esterno: `npm install -g @musistudio/claude-code-router`; `ccr start` per il proxy locale | MIT; repo musistudio/claude-code-router |
| `claude-mem` | Memoria semantica persistente cross-sessione (SQLite + Chroma, tutto locale); tre livelli di retrieval (indice, timeline, dettaglio) per non far esplodere il contesto al riavvio | Progetti multi-settimana con storia architetturale e debug history che il deterministico `.claude/memory/` non scala a gestire; i due approcci sono complementari, non alternativi | Tool esterno: `npx claude-mem install`; web viewer locale su `localhost:37777`; tag `<private>` per escludere contenuto sensibile | MIT; repo thedotmack/claude-mem |
| `andrej-karpathy-skills` | Skill con quattro principi comportamentali anti-over-engineering: no assunzioni silenziose, no 500 righe dove ne bastano 50, no modifiche ortogonali, no scope creep | Qualsiasi progetto con output di codice intenso; zero dipendenze runtime; complementare a `rules/interaction-style.md` | Skill esterna: clonare in `.claude/skills/andrej-karpathy-skills/` | MIT; repo forrestchang/andrej-karpathy-skills |
| `code-simplifier` | Passa il codice gia scritto attraverso un filtro di semplificazione con vincolo esplicito contro il cambio di comportamento; sicuro da eseguire prima del merge | Progetti dove l'over-engineering e' un rischio ricorrente; da usare selettivamente su output genuinamente complesso, non come passo di routine | Plugin ufficiale Anthropic: `/plugin marketplace add anthropics/claude-plugins-official` poi `/plugin install code-simplifier` | Ufficiale Anthropic; marketplace anthropics/claude-plugins-official |
| `github-mcp` (MCP) | Issue, PR, code search, gestione repository GitHub direttamente da Claude; il singolo MCP a maggiore impatto per chi usa GitHub | Qualsiasi progetto su GitHub con workflow di PR e issue attivo | `.mcp.json` in radice con configurazione del server GitHub ufficiale | MIT; implementazione ufficiale GitHub; repo github/github-mcp-server |
| `context7` (MCP) | Documentazione aggiornata di librerie e framework con query in un singolo trigger; risolve le allucinazioni da documentazione obsoleta su framework con release frequenti | Progetti che usano framework in evoluzione rapida (Next.js, React, FastAPI, ecc.) | `.mcp.json` in radice con configurazione Context7 | MIT; repo upstash/context7 |
| `playwright-mcp` (MCP) | Automazione browser via accessibility snapshot (non screenshot), test E2E e verifica visuale controllata; gira interamente in locale | Progetti con test E2E o con verifica visuale dell'interfaccia come parte del workflow di sviluppo | `.mcp.json` in radice con configurazione Playwright MCP | MIT; implementazione ufficiale Microsoft; repo microsoft/playwright-mcp |
| `sentry-mcp` (MCP) | Stack trace, breadcrumb e correlazione errori-release direttamente in Claude; elimina il loop copia-incolla del contesto di errore | Progetti con error monitoring attivo su Sentry | `.mcp.json` in radice con configurazione Sentry MCP (HTTP + OAuth) | Implementazione ufficiale Sentry |
| `figma-mcp` (MCP) | Accesso all'albero dei nodi Figma per implementare design pixel-accurate senza copia-incolla manuale | Progetti frontend con design file Figma come source of truth | `.mcp.json` in radice con configurazione Figma MCP (HTTP + OAuth) | Implementazione ufficiale Figma |
| `vercel-mcp` (MCP) | Monitoraggio deploy, gestione variabili d'ambiente, health check produzione vs preview | Progetti deployed su Vercel con workflow di deploy attivo; rischio medio perche' puo modificare env var | `.mcp.json` in radice con configurazione Vercel MCP (HTTP + OAuth) | Implementazione ufficiale Vercel |
| `linear-mcp` (MCP) | CRUD issue, sprint e cicli Linear direttamente da Claude | Team che usa Linear come issue tracker; richiede piano Standard+ | `.mcp.json` in radice con configurazione Linear MCP (HTTP + OAuth) | Implementazione ufficiale Linear |
| `zotero-mcp` (MCP) | Libreria bibliografica di lavoro: aggiunta per DOI, ricerca semantica, export BibTeX verso `research-vault/bibliography.bib` | Progetti con il pacchetto `academic-researcher` attivo che gestiscono una libreria Zotero | `.mcp.json` in radice con configurazione del server Zotero scelto | Community, non ufficiale Zotero; raccomandato `54yyyu/zotero-mcp` (semantic search, CLI), alternativa `Xevos117/mcp-zotero` se serve iniezione citazioni in `.docx` |
| `academix` (MCP) | Ricerca letteratura aggregata su OpenAlex, DBLP, Semantic Scholar, arXiv e Crossref, con export BibTeX nativo | Progetti con `academic-researcher` attivo, come base della skill `literature-search` | `.mcp.json` in radice con configurazione Academix | Community; repo xingyulu23/Academix |
| `semantic-scholar-mcp` (MCP) | Ricerca su 225M+ paper con `export_bibtex` nativo e tracking di sessione (`list_tracked_papers`) | Progetti con `academic-researcher` attivo, in combinazione con `academix` | `.mcp.json` in radice con configurazione semantic-scholar-mcp | Community; repo akapet00/semantic-scholar-mcp |
| `refchecker-mcp` (MCP) | Verifica anti-hallucination delle citazioni: `verify_citation` controlla titolo/autori/anno/DOI/arXiv-ID contro Semantic Scholar, OpenAlex e Crossref | Progetti con `academic-researcher` attivo; usato dalla skill `citation-tracker`, con fallback manuale se assente | `.mcp.json` in radice con configurazione refchecker-mcp | Community; repo JonasBaath/mcp-refchecker, costruito su academic-refchecker (MIT) |
| `subagent-template` | Quattro agenti di esempio pronti all'uso (`code-reviewer`, `security-auditor`, `debugger`, `explorer`) con frontmatter YAML e system prompt strutturato; base per definire ruoli agente specializzati del progetto | Progetti che richiedono review automatizzata, audit di sicurezza ricorrenti o investigazione sistematica dei bug, o dove si vogliono definire subagent riusabili; si scelgono uno per uno | `templates/agents/<agente>.md` in `.claude/agents/` del progetto | Documentazione ufficiale Claude Code, formato nativo; `debugger` ed `explorer` derivano dal bundle Cranot dell'utente, ed `explorer` duplica in parte l'agente nativo Explore (la variante di progetto e' versionata e conosce il sistema di memoria): la sovrapposizione si dichiara al gate |
| `agent-catalog` | Pesca a richiesta, uno alla volta, subagent specializzati per stack/framework dalla fonte community `0xfurai/claude-code-subagents` (138 agenti, file Markdown flat senza plugin), con tre comandi (`/list-community-agents`, `/fetch-community-agent`, `/check-agent-sources`) e script dual-OS che confrontano il commit della fonte con quello registrato, sulla stessa strategia a costo zero di `claude-code-handoff` | Il progetto vuole un subagent iper-specifico per uno stack o un tool preciso senza scriverlo da zero, e la fonte scelta non offre un plugin nativo installabile; si propone quando emerge un bisogno puntuale (es. "serve un agente esperto di Prisma") | `templates/agent-catalog/` in `.claude/agent-catalog/sources.json` (tracciato), `.claude/commands/` (3 comandi), `tools/{list,fetch,check}-community-agent{,-sources}.ps1`/`.sh`; stato generato in `.claude/agent-catalog/state.json` (tracciato); ogni fetch scrive in `.claude/agents/<agente>.md` | Fonte `0xfurai/claude-code-subagents` (MIT) integrata come fetchable; l'indice curato `hesreallyhim/awesome-claude-code` e' tracciato come fonte "meta" solo per il controllo di drift, non elencabile; per `wshobson/agents` e `VoltAgent/awesome-claude-code-subagents`, che offrono gia' un plugin marketplace nativo, si usano le righe di catalogo separate sotto invece del fetch di questo pacchetto; meccanismo verificato dal vivo due volte (in isolamento e dentro un progetto ospite reale nel pilota del 2026-07-02), entrambe pulite |
| `wshobson-agents` | Marketplace di plugin Claude Code nativo con 194 agent, 158 skill e 106 comandi slash organizzati per dominio (linguaggi, infrastruttura, security, dati/ML) | Il progetto vuole installare unita' complete e curate di agent/skill per dominio invece di pescare singoli file, e preferisce l'aggiornamento gestito dal plugin marketplace nativo | Plugin Claude Code: `/plugin marketplace add wshobson/agents` poi `/plugin install <plugin>@claude-code-workflows` (nome del plugin dal proprio catalogo) | MIT; repo wshobson/agents, 37,4k stelle, il piu' mantenuto tra le fonti esaminate |
| `voltagent-subagents` | Marketplace di plugin Claude Code nativo con 154+ subagent in 10 categorie (core development, language specialists, infrastructure, quality & security, data & AI, developer experience, specialized domains, business & product, meta-orchestration, research & analysis) | Il progetto vuole installare un intero dominio di subagent gia' curato (es. tutti gli specialist di linguaggio) invece di pescarli uno a uno | Plugin Claude Code: `/plugin marketplace add VoltAgent/awesome-claude-code-subagents` poi `/plugin install <plugin>@voltagent-subagents` (un plugin per categoria, nomi nel proprio `marketplace.json`) | MIT; repo VoltAgent/awesome-claude-code-subagents, 22,7k stelle |
| `multiclaude` | Supervisore che assegna task a subagent definiti via file Markdown; modalita singleplayer (merge automatico) o multiplayer (review umana richiesta prima del merge) | Progetti con 3 o piu feature branch paralleli dove si vuole supervisione con gate di review umana | Tool esterno: `go install github.com/dlorenc/multiclaude/cmd/multiclaude@latest` | MIT; repo dlorenc/multiclaude |
| `psmux` | Tmux nativo per Windows (no WSL, no Cygwin); legge il `.tmux.conf` esistente; first-class support per le agent teams di Claude Code in pannelli separati | Windows senza WSL2 con 3 o piu sessioni Claude Code in parallelo | Tool esterno per l'utente; nessun file nel progetto | MIT; repo psmux/psmux |
| `tmux-claude-session-manager` | Plugin tmux con picker centrale (fzf) che mostra lo stato live (working/waiting/idle) di tutte le sessioni Claude Code, alimentato dagli hook nativi `UserPromptSubmit` e `Stop`; no polling | Setup con tmux e 3 o piu sessioni Claude Code contemporanee | Plugin tmux (TPM): configurazione in `.tmux.conf`; nessun file nel progetto | MIT; repo craftzdog/tmux-claude-session-manager |
| `claudecode.nvim` | Plugin Neovim che implementa il protocollo MCP WebSocket delle estensioni ufficiali (VS Code, JetBrains) in Lua pura; toggle floating window, auto-reload buffer quando Claude modifica file su disco | Workflow primario in Neovim | Configurazione nel `init.lua` dell'utente; nessun file nel progetto | MIT; repo coder/claudecode.nvim; alternativa con toggle piu semplice: greggh/claude-code.nvim |

## Aggiungere un pacchetto

Un nuovo pacchetto si aggiunge con una riga in questo catalogo e, se e' a cartella, con una sottocartella sotto `templates/` che contiene un proprio `README.md` di istanziazione, sul modello di `templates/latex/`. La colonna "quando offrirlo" deve indicare un trigger concreto, cosi che il gate sappia quando proporlo senza assumere. I pacchetti che non sono cartelle, come `diagrams` e `code-context`, vivono come voci del catalogo e puntano ai file gia presenti sotto `templates/`.

Il percorso completo per estendere il sistema, dalla ricerca dello strumento alla voce di catalogo fino alla validazione come case-study, e' descritto nella sezione "Come estendere il sistema" del `README.md` di radice.
